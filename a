-- Fishing & Teleport GUI v4 - WORKING INSTANT FISH AND REEL FOR THIS GAME

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

local remote = ReplicatedStorage:WaitForChild("ffrostflame_bridgenet2@1.0.0"):WaitForChild("dataRemoteEvent")
local USER_ID = LocalPlayer.UserId

-- CONFIG: Set these per your net, or make them dynamic if you want
local FISH_PER_NET = 4           -- Max fish your net can hold
local TOTAL_REEL_PRESSES = 12    -- How many times you'd normally have to click to reel

-- The latest vector position for the water, from your log
local FISH_VECTOR = Vector3.new(283.98358154296875, 27.99997329711914, -204.96829223632812)

-- List of fish to use, from your log. Can randomize or just cycle.
local FISH_TYPES = {"RainbowTrout", "Perch", "Carp"}

-- Float values to use (randomized or cycled)
local FLOATS = {1.18, 1.35, 1.13, 1.71}

-- Status
local attractActive = false
local reelActive = false

-- GUI Setup (as before, tabs + scrollable + minimize/maximize)
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "FishingGUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = game:GetService("CoreGui")

local window = Instance.new("Frame")
window.Name = "MainFrame"
window.Size = UDim2.new(0, 420, 0, 320)
window.Position = UDim2.new(0.5, -210, 0.5, -160)
window.BackgroundColor3 = Color3.fromRGB(42, 44, 51)
window.BorderSizePixel = 0
window.Parent = screenGui
window.Active = true
window.Draggable = true

local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 38)
titleBar.BackgroundColor3 = Color3.fromRGB(55, 57, 62)
titleBar.Parent = window

local title = Instance.new("TextLabel")
title.Text = "Fishing & Teleport GUI"
title.Font = Enum.Font.GothamBold
title.TextSize = 19
title.TextColor3 = Color3.new(1,1,1)
title.BackgroundTransparency = 1
title.Size = UDim2.new(1, -80, 1, 0)
title.Position = UDim2.new(0, 12, 0, 0)
title.Parent = titleBar

local minBtn = Instance.new("TextButton")
minBtn.Text = "_"
minBtn.Size = UDim2.new(0, 38, 1, 0)
minBtn.Position = UDim2.new(1, -76, 0, 0)
minBtn.Font = Enum.Font.GothamBold
minBtn.TextSize = 28
minBtn.BackgroundColor3 = Color3.fromRGB(80,80,80)
minBtn.TextColor3 = Color3.new(1,1,1)
minBtn.Parent = titleBar

local maxBtn = Instance.new("TextButton")
maxBtn.Text = "⬜"
maxBtn.Size = UDim2.new(0, 38, 1, 0)
maxBtn.Position = UDim2.new(1, -38, 0, 0)
maxBtn.Font = Enum.Font.GothamBold
maxBtn.TextSize = 22
maxBtn.BackgroundColor3 = Color3.fromRGB(80,80,80)
maxBtn.TextColor3 = Color3.new(1,1,1)
maxBtn.Parent = titleBar

local minimized = false

local tabBtnHolder = Instance.new("Frame")
tabBtnHolder.Size = UDim2.new(1, 0, 0, 38)
tabBtnHolder.Position = UDim2.new(0,0,0,38)
tabBtnHolder.BackgroundTransparency = 1
tabBtnHolder.Parent = window

local tabNames = {"Fishing", "Sell Teleport", "Zone Teleport", "Boss Teleport"}
local tabs = {}
local tabBtns = {}
for i, tabName in ipairs(tabNames) do
    local btn = Instance.new("TextButton")
    btn.Text = tabName
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 16
    btn.Size = UDim2.new(0, 105, 1, 0)
    btn.Position = UDim2.new(0, (i-1)*105, 0, 0)
    btn.BackgroundColor3 = Color3.fromRGB(65,65,65)
    btn.TextColor3 = Color3.new(1,1,1)
    btn.Parent = tabBtnHolder
    tabBtns[i] = btn

    local tab = Instance.new("Frame")
    tab.Name = tabName:gsub(" ", "").."Tab"
    tab.BackgroundTransparency = 1
    tab.Size = UDim2.new(1, 0, 1, -76)
    tab.Position = UDim2.new(0, 0, 0, 76)
    tab.Visible = (i == 1)
    tab.Parent = window
    tabs[i] = tab
end

local function showTab(idx)
    if minimized then return end
    for i, tab in ipairs(tabs) do
        tab.Visible = (i == idx)
    end
    tabBtnHolder.Visible = true
end
for i, btn in ipairs(tabBtns) do
    btn.MouseButton1Click:Connect(function()
        showTab(i)
    end)
end

minBtn.MouseButton1Click:Connect(function()
    minimized = true
    for _, tab in ipairs(tabs) do tab.Visible = false end
    tabBtnHolder.Visible = false
    window.Size = UDim2.new(0,420,0,38)
end)
maxBtn.MouseButton1Click:Connect(function()
    minimized = false
    window.Size = UDim2.new(0,420,0,320)
    tabBtnHolder.Visible = true
    showTab(1)
end)
showTab(1)

-- Status function (updates ON/OFF color)
local attractStatus, reelStatus
local function updateStatus()
    if attractStatus then
        attractStatus.Text = attractActive and "ON" or "OFF"
        attractStatus.TextColor3 = attractActive and Color3.fromRGB(60, 220, 110) or Color3.fromRGB(220, 60, 60)
    end
    if reelStatus then
        reelStatus.Text = reelActive and "ON" or "OFF"
        reelStatus.TextColor3 = reelActive and Color3.fromRGB(60, 220, 110) or Color3.fromRGB(220, 60, 60)
    end
end

-- FISHING TAB
local fishingTab = tabs[1]

local attractBtn = Instance.new("TextButton")
attractBtn.Text = "Instant Attract Fish"
attractBtn.Font = Enum.Font.GothamBold
attractBtn.TextSize = 16
attractBtn.Size = UDim2.new(0, 240, 0, 40)
attractBtn.Position = UDim2.new(0, 25, 0, 35)
attractBtn.BackgroundColor3 = Color3.fromRGB(60, 180, 90)
attractBtn.TextColor3 = Color3.new(1,1,1)
attractBtn.Parent = fishingTab

attractStatus = Instance.new("TextLabel")
attractStatus.Size = UDim2.new(0, 50, 0, 40)
attractStatus.Position = UDim2.new(0, 275, 0, 35)
attractStatus.Text = "OFF"
attractStatus.Font = Enum.Font.GothamBold
attractStatus.TextSize = 16
attractStatus.BackgroundTransparency = 1
attractStatus.TextColor3 = Color3.fromRGB(220,60,60)
attractStatus.Parent = fishingTab

local reelBtn = Instance.new("TextButton")
reelBtn.Text = "Instant Reel"
reelBtn.Font = Enum.Font.GothamBold
reelBtn.TextSize = 16
reelBtn.Size = UDim2.new(0, 240, 0, 40)
reelBtn.Position = UDim2.new(0, 25, 0, 85)
reelBtn.BackgroundColor3 = Color3.fromRGB(60, 130, 250)
reelBtn.TextColor3 = Color3.new(1,1,1)
reelBtn.Parent = fishingTab

reelStatus = Instance.new("TextLabel")
reelStatus.Size = UDim2.new(0, 50, 0, 40)
reelStatus.Position = UDim2.new(0, 275, 0, 85)
reelStatus.Text = "OFF"
reelStatus.Font = Enum.Font.GothamBold
reelStatus.TextSize = 16
reelStatus.BackgroundTransparency = 1
reelStatus.TextColor3 = Color3.fromRGB(220,60,60)
reelStatus.Parent = fishingTab

local info = Instance.new("TextLabel")
info.Text = "Press the buttons above to instantly attract fish or reel in.\n(C and R hotkeys still work)"
info.Font = Enum.Font.Gotham
info.TextSize = 14
info.Size = UDim2.new(1, -20, 0, 40)
info.Position = UDim2.new(0, 10, 0, 135)
info.TextColor3 = Color3.new(1,1,1)
info.BackgroundTransparency = 1
info.Parent = fishingTab

updateStatus()

-- Sell/Zone/Boss Tabs (scrollable)
local function populateTeleportTab(tab, src, color)
    local scroll = Instance.new("ScrollingFrame")
    scroll.Size = UDim2.new(1, -20, 1, -20)
    scroll.Position = UDim2.new(0, 10, 0, 10)
    scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    scroll.BackgroundTransparency = 1
    scroll.ScrollBarThickness = 8
    scroll.Parent = tab

    local layout = Instance.new("UIListLayout")
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Padding = UDim.new(0, 6)
    layout.Parent = scroll

    if src then
        for _, obj in pairs(src:GetChildren()) do
            if obj:IsA("BasePart") or obj:IsA("Model") then
                local btn = Instance.new("TextButton")
                btn.Size = UDim2.new(1, -10, 0, 35)
                btn.Text = "Teleport to "..obj.Name
                btn.BackgroundColor3 = color
                btn.TextColor3 = Color3.new(1,1,1)
                btn.Font = Enum.Font.Gotham
                btn.TextSize = 16
                btn.Parent = scroll
                btn.MouseButton1Click:Connect(function()
                    local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
                    if obj:IsA("BasePart") then
                        char:MoveTo(obj.Position)
                    elseif obj:IsA("Model") and obj.PrimaryPart then
                        char:MoveTo(obj.PrimaryPart.Position)
                    else
                        local part = obj:FindFirstChildWhichIsA("BasePart")
                        if part then
                            char:MoveTo(part.Position)
                        end
                    end
                end)
            end
        end
        wait() -- Allow UIListLayout to update
        scroll.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 12)
    else
        local txt = Instance.new("TextLabel")
        txt.Text = "Nothing found!"
        txt.Size = UDim2.new(1, -10, 0, 40)
        txt.Font = Enum.Font.Gotham
        txt.TextColor3 = Color3.fromRGB(200,50,50)
        txt.BackgroundTransparency = 1
        txt.Parent = scroll
        scroll.CanvasSize = UDim2.new(0, 0, 0, 50)
    end
end
populateTeleportTab(tabs[2], workspace:FindFirstChild("GameStalls"), Color3.fromRGB(120,90,90))
populateTeleportTab(tabs[3], workspace:FindFirstChild("Zones"), Color3.fromRGB(90,120,90))
populateTeleportTab(tabs[4], ReplicatedStorage:FindFirstChild("Assets") and ReplicatedStorage.Assets:FindFirstChild("BossEvent"), Color3.fromRGB(90,90,120))

-- INSTANT FISH LOGIC
local rng = Random.new()
local function doInstantAttract()
    attractActive = true
    updateStatus()
    -- Simulate all fish being caught instantly for your net
    for i = 1, FISH_PER_NET do
        local fishType = FISH_TYPES[((i - 1) % #FISH_TYPES) + 1]
        local floatValue = FLOATS[((i - 1) % #FLOATS) + 1]
        local args = {
            {
                {
                    "FishCaptureEffect",
                    {
                        fishType,
                        floatValue,
                        FISH_VECTOR,
                        USER_ID
                    }
                },
                "!"
            }
        }
        remote:FireServer(unpack(args))
        wait(0.1)
    end
    attractActive = false
    updateStatus()
end

local function doInstantReel()
    reelActive = true
    updateStatus()
    -- Instantly do all reel clicks
    for i = 1, TOTAL_REEL_PRESSES do
        local args = {
            {1, "9"}
        }
        remote:FireServer(unpack(args))
        wait(0.05) -- safe delay (20/s)
    end
    reelActive = false
    updateStatus()
end

attractBtn.MouseButton1Click:Connect(doInstantAttract)
reelBtn.MouseButton1Click:Connect(doInstantReel)

game:GetService("UserInputService").InputBegan:Connect(function(input, processed)
    if processed then return end
    if input.KeyCode == Enum.KeyCode.C then
        doInstantAttract()
    elseif input.KeyCode == Enum.KeyCode.R then
        doInstantReel()
    end
end)
